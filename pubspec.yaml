name: flutter_application_1
description: PM-AJAY Infrastructure Gap Identification System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3 # <-- Add this line

  # Navigation
  go_router: ^13.2.0

  # UI & UX
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9

  # Utilities
  intl: ^0.18.1

  collection: ^1.18.0

  #map
  flutter_map: ^6.1.0
  latlong2: ^0.9.0 # A helper package for flutter_map
  geolocator: ^11.0.0

  #url
  url_launcher: ^6.2.5

  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  riverpod_generator: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/chatbot/